
import 'package:flame/components.dart';

class ForestSpark extends SpriteAnimationComponent {
  
  ForestSpark.fromSpriteAnimation(size, animation) : super.fromSpriteAnimation(size, animation); 
  @override
  void update(double dt) {
    super.update(dt);
 
  }

}
